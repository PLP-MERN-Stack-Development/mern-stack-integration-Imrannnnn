# Client (Vite) environment variables example
# Vite only exposes variables that start with VITE_ to the browser.
# Copy this file to `.env` in the `client/` folder and adjust values as needed.

# Full API URL used by the client to reach the server API
VITE_API_URL=http://localhost:5000/api

# Optional: front-end feature flags
VITE_ENABLE_DEBUG=false

# Server environment variables example
# Copy this file to `.env` in the `server/` folder and replace values as needed.

PORT=5000
MONGO_URI=mongodb://localhost:27017/mern_blog
JWT_SECRET=your_jwt_secret_here
CLIENT_URL=http://localhost:5173

# Optional settings
# If you store uploads locally, this is the relative folder used by multer
UPLOADS_DIR=uploads
